# Feedback on Project Presentation

**Rafik**

Use the fact that we are in **AR** to add vision features with an LLM, etc.
For example, if a server is down, the system could draw information directly
on the screen.

Since we have an agent installed on the servers, we could use it to perform
actions and retrieve information from the server, possibly with LLM
integration too.

The solution should not be limited to the local network. We should have a public
infrastructure so the connection works from anywhere on the internet.

Should we build it for a phone instead of Meta Quest?
